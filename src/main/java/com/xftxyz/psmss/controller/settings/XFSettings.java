package com.xftxyz.psmss.controller.settings;

// 这里没有使用interface，因为数据可以通过设置更改
public class XFSettings {
    public static int DEFAULT_IDLE_QUEUE_CAPACITY = 30;
    public static int DEFAULT_READY_QUEUE_CAPACITY = 30;
    public static int DEFAULT_BLOCKED_QUEUE_CAPACITY = 30;

}
